#!/usr/bin/env bash

if [ -d $HOME/.linuxbrew ]; then
    echo "Linuxbrew is already installed in ~/.linuxbrew" 1>&2;
    exit 1;
fi

echo "INSTALLING."
git clone https://github.com/Linuxbrew/brew.git ${HOME}/.linuxbrew

echo "DONE. Go ahead an use it."

