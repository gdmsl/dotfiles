# Select the prompt from the standards
# use prompt -l or prompt -p to list and preview them
prompt grml

# User defined aliases
alias feh='feh --draw-filename --draw-tinted --force-aliasing --image-bg black --scale-down'

alias betty="~/Sviluppo/github/betty/main.rb"
