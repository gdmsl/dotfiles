#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

# If you want to use GNOME config tools...
#
#if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
#  /usr/lib/openbox/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null 2>&1; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &

# load the Xresources
( sleep 1s && xrdb ~/.Xresources ) &

# set the laptop monitor as default (primary)
( sleep 1s && xrandr --output LVDS1 --primary ) &

# Solid color background
( sleep 1s && xsetroot -solid "#050505" ) &

# Clipit
( sleep 1s && clipit ) &

# Screen off after 5 minutes
( sleep 1s && xset dpms 600 ) &

# Feh background
( sleep 1s && sh ~/.fehbg ) &

# Compositor
( sleep 1s && compton --config ~/.config/compton/compton-openbox.conf -b ) &

# sound icon
( sleep 1s && volumeicon ) &

# nm-applet
( sleep 1s && nm-applet ) &

# tint2
( sleep 1s && tint2 ) &

# start conky
# ( sleep 3s && conky ) &

