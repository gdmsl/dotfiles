# `use flake` is provided by nix-direnv. It evaluates the flake's default
# devShell and exports its environment back to your shell. Results are cached
# under .direnv/ so subsequent `cd`s in are near-instant — the flake is only
# re-evaluated when flake.nix or flake.lock changes.
use flake
